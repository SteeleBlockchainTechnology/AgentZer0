[project]
name = "agentzer0"
version = "0.1.0"
description = "A proactive Discord bot for a trading community"
authors = [
    {name = "Your Name", email = "you@example.com"}
]
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "discord-py>=2.5.2,<3.0.0",
    "python-dotenv>=1.1.0,<2.0.0",
    "groq>=0.23.1,<0.24.0",
    "crewai>=0.118.0,<0.119.0",  # Explicit version range for clarity
    "langchain-groq (>=0.3.2,<0.4.0)",
    "fastembed (>=0.6.1,<0.7.0)",
    "onnxruntime (>=1.21.1,<2.0.0)",
    "langchain-mcp-adapters (>=0.0.9,<0.0.10)",
    "langchain (>=0.3.24,<0.4.0)",
    "langgraph (>=0.4.1,<0.5.0)"
]

[tool.poetry]
# Empty section, retained for future use if needed

[tool.poetry.group.dev.dependencies]
pytest = "^8.3.5"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"